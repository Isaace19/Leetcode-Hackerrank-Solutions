class Solution {
public:
    int minSteps(string s, string t) {
        // error check that the strings are the same length
        if (s != t ) {
            std::cout << "can't form an anagram";
            return 1;
        }

        // hashmap the strings together and detect anagrams



    }
};
